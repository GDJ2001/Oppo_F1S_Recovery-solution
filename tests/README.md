# Tests

Tests for recovery workflow logic, device detection, configuration parsing, and packaging scripts.

- `unit/`: isolated tests
- `integration/`: tests that exercise multiple modules or external tool wrappers
- `fixtures/`: safe sample outputs and config files
