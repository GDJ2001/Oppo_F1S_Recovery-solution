# SP Flash Tool

Place the local SP Flash Tool v5 runtime here. Tool binaries are ignored by Git.

The workflow expects `flash_tool.exe` under this folder when launching SP Flash
Tool for a validated scatter firmware package.
